---
---
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Happy Valentine's Day My Love ❤️</title>
    <style>
        body, html {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
            overflow: hidden; 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #fff0f3 0%, #ffccd5 100%);
        }
        
        #main-container {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            width: 100vw;
            position: relative;
        }

        @keyframes float {
            0% { transform: translateY(0) rotate(0deg); opacity: 1; }
            100% { transform: translateY(-100vh) rotate(360deg); opacity: 0; }
        }

        .heart {
            position: fixed; bottom: -20px; font-size: 20px; color: #ff4d6d;
            user-select: none; pointer-events: none; animation: float 5s linear forwards; z-index: 50;
        }

        .card {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            padding: 25px;
            border-radius: 30px;
            box-shadow: 0 15px 35px rgba(255, 77, 109, 0.2);
            width: 85%;
            max-width: 400px;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.5);
            z-index: 100;
        }

        .slides-container {
            width: 100%;
            height: 300px;
            border-radius: 20px;
            overflow: hidden;
            margin-bottom: 15px;
            border: 2px solid #ffccd5;
        }

        .mySlides {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: none;
            animation: fadeEffect 1s;
        }

        @keyframes fadeEffect { from {opacity: 0.4} to {opacity: 1} }

        input {
            width: 100%;
            padding: 12px;
            margin: 15px 0;
            border: 2px solid #ffccd5;
            border-radius: 12px;
            text-align: center;
            font-size: 16px;
            outline: none;
        }

        button {
            background: #ff4d6d;
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 12px;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
        }
    </style>
</head>
<body>

    <audio id="bgMusic" loop>
        <source src="https://files.catbox.moe/k2r0sh.mp3" type="audio/mpeg">
    </audio>

    <div id="main-container">
        <div id="lock" class="card">
            <div style="font-size:50px;">🔒</div>
            <h2 style="color:#ff4d6d;">جاهزة للمفاجأة؟</h2>
            <p style="color:#666;">اكتبي تاريخ احسن يوم في عمري 😉</p>
            <input type="tel" id="pass" placeholder="أدخلي التاريخ هنا..">
            <button id="go" style="width:100%;">افتحي قلبي ❤️</button>
            <p id="err" style="color:#ff4d6d; display:none; margin-top:10px; font-weight:bold;">التاريخ غلط يا توتا.. ركزي 🥺</p>
        </div>

        <div id="show" class="card" style="display:none;">
            <h1 style="color:#ff4d6d; font-size:24px;">Happy Valentine's Day ❤️</h1>
            
            <div class="slides-container">
                <img class="mySlides" src="https://i.postimg.cc/pL4dZL4M/our-photo.jpg">
                <img class="mySlides" src="https://i.postimg.cc/6pgQ0pgX/our-photo.jpg">
                <img class="mySlides" src="https://i.postimg.cc/wjZBVjPZ/our-photo-2.jpg">
                <img class="mySlides" src="https://i.postimg.cc/6pgQ0pgK/Screenshot-20260215-061957-Contacts.jpg">
            </div>

            <p style="color:#444; font-size:16px; line-height:1.5;">
                كل صورنا سوا حكاية، وكل يوم وانتي معايا عيد.. 
                <br>
                <span style="color:#ff4d6d; font-weight:bold; font-size:20px;">Will you be my valentine? 🥺</span>
            </p>
            
            <div id="btns-container" style="display:flex; justify-content:center; gap:15px; margin-top:15px;">
                <button id="yesBtn">Yes 😍</button>
                <button id="noBtn" style="background:#eee; color:#888;">No 😢</button>
            </div>
            
            <div id="final" style="display:none; margin-top:15px; color:#ff4d6d; font-weight:bold; border:2px dashed #ff4d6d; padding:10px; border-radius:15px; background:#fff0f3;">
                بحبك ي توتاا ي قلب عبده من جوه ❤️💍
            </div>
        </div>
    </div>

    <script is:inline>
        let slideIndex = 0;
        function showSlides() {
            let slides = document.getElementsByClassName("mySlides");
            for (let i = 0; i < slides.length; i++) { slides[i].style.display = "none"; }
            slideIndex++;
            if (slideIndex > slides.length) {slideIndex = 1}    
            slides[slideIndex-1].style.display = "block";  
            setTimeout(showSlides, 3000);
        }

        function startHearts() {
            setInterval(() => {
                const heart = document.createElement('div');
                heart.innerHTML = '❤️';
                heart.className = 'heart';
                heart.style.left = Math.random() * 100 + 'vw';
                heart.style.animationDuration = (Math.random() * 2 + 3) + 's';
                document.body.appendChild(heart);
                setTimeout(() => heart.remove(), 5000);
            }, 400);
        }

        document.getElementById('go').onclick = function() {
            const pass = document.getElementById('pass').value;
            if (pass === '15112021') {
                document.getElementById('lock').style.display = 'none';
                document.getElementById('show').style.display = 'block';
                document.getElementById('bgMusic').play();
                showSlides();
                startHearts();
            } else {
                document.getElementById('err').style.display = 'block';
            }
        };

        document.getElementById('yesBtn').onclick = function() {
            document.getElementById('final').style.display = 'block';
            document.getElementById('btns-container').style.display = 'none';
        };

        const noBtn = document.getElementById('noBtn');
        const moveNo = () => {
            noBtn.style.position = 'fixed';
            const x = Math.random() * (window.innerWidth - 100);
            const y = Math

---
// index.astro
---

<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إلى أشرقت ❤️</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root { --main-purple: #ff00ff; --dark-bg: #0d0221; --card-bg: #1a1a2e; }
        body { background-color: var(--dark-bg); color: white; font-family: 'Cairo', sans-serif; margin: 0; display: flex; justify-content: center; align-items: center; min-height: 100vh; overflow-x: hidden; }
        
        /* شاشة الدخول - ظاهرة دائماً في البداية */
        #login-screen { text-align: center; width: 90%; max-width: 350px; background: var(--card-bg); padding: 40px 30px; border-radius: 35px; box-shadow: 0 0 30px rgba(255, 0, 255, 0.4); position: fixed; z-index: 9999; }
        .input-box { width: 100%; padding: 15px; margin: 20px 0; border-radius: 50px; border: 2px solid var(--main-purple); background: transparent; color: #fff; text-align: center; font-size: 1.1rem; outline: none; }
        .login-btn { width: 100%; padding: 15px; border-radius: 50px; border: none; background: linear-gradient(90deg, #ff00ff, #8a2be2); color: #fff; font-weight: bold; cursor: pointer; font-size: 1.1rem; }

        /* المحتوى - مخفي تماماً */
        #main-content { display: none; opacity: 0; text-align: center; width: 95%; max-width: 500px; padding: 20px 0; transition: opacity 1s; }
        
        .counter-section { background: rgba(255, 255, 255, 0.05); padding: 15px; border-radius: 20px; margin-bottom: 20px; border: 1px solid rgba(255, 0, 255, 0.2); }
        .counter-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; direction: ltr; }
        .counter-box { background: var(--card-bg); padding: 10px 5px; border-radius: 12px; }
        .counter-box span { display: block; font-size: 1.2rem; font-weight: bold; color: var(--main-purple); }

        .slider { position: relative; width: 100%; height: 350px; border-radius: 25px; overflow: hidden; border: 3px solid var(--main-purple); margin-bottom: 25px; }
        .slider img { position: absolute; width: 100%; height: 100%; object-fit: cover; opacity: 0; transition: opacity 1s; top: 0; left: 0; }
        .slider img.active { opacity: 1; }

        .video-container { width: 100%; max-width: 315px; margin: 20px auto; border-radius: 20px; overflow: hidden; }
        iframe { width: 100%; height: 560px; border: none; }

        .heart { position: fixed; color: #ff00ff; font-size: 20px; z-index: 1; top: -10%; animation: fall linear forwards; }
        @keyframes fall { to { transform: translateY(110vh) rotate(360deg); opacity: 0; } }
    </style>
</head>
<body>

    <div id="login-screen">
        <h1>وحشتيني يا أشرقت 💕</h1>
        <p style="color: #b3b3b3;">اكتبي تاريخنا عشان تدخلي 💜</p>
        <input type="tel" id="passInput" class="input-box" placeholder="15112021">
        <button id="goBtn" class="login-btn">فتح قلبي 🔓</button>
    </div>

    <div id="main-content">
        <h1>إلى أشرقت ❤️</h1>
        <div class="counter-section">
            <p>حبنا بقاله:</p>
            <div class="counter-grid">
                <div class="counter-box"><span id="years">0</span><p>سنين</p></div>
                <div class="counter-box"><span id="months">0</span><p>شهور</p></div>
                <div class="counter-box"><span id="days">0</span><p>أيام</p></div>
                <div class="counter-box"><span id="hours">0</span><p>ساعات</p></div>
                <div class="counter-box"><span id="minutes">0</span><p>دقائق</p></div>
                <div class="counter-box"><span id="seconds">0</span><p>ثواني</p></div>
            </div>
        </div>

        <div class="slider" id="slider">
            <img src="/1.jpg" class="active">
            <img src="/2.jpg">
            <img src="/3.jpg">
            <img src="/4.jpg">
        </div>

        <div class="video-container">
            <iframe id="youtube-vid" src="https://www.youtube.com/embed/n7Tmcj8E6qQ?enablejsapi=1" allow="autoplay"></iframe>
        </div>

        <p style="padding: 20px;">أشرقت يعني قلبي وروحي وعقلي.. كل سنة وإنتي معايا ❤️</p>
    </div>

    <script is:inline>
        // 1. وظيفة العداد
        function updateCounter() {
            const start = new Date("2021-11-15T00:00:00");
            const now = new Date();
            const diff = now - start;
            document.getElementById("years").innerText = Math.floor(diff / (1000 * 60 * 60 * 24 * 365.25));
            document.getElementById("months").innerText = Math.floor((diff % (1000 * 60 * 60 * 24 * 365.25)) / (1000 * 60 * 60 * 24 * 30.44));
            document.getElementById("days").innerText = Math.floor((diff % (1000 * 60 * 60 * 24 * 30.44)) / (1000 * 60 * 60 * 24));
            document.getElementById("hours").innerText = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            document.getElementById("minutes").innerText = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
            document.getElementById("seconds").innerText = Math.floor((diff % (1000 * 60)) / 1000);
        }

        // 2. وظيفة السلايدر
        function startImages() {
            let current = 0;
            const imgs = document.querySelectorAll('.slider img');
            setInterval(() => {
                imgs[current].classList.remove('active');
                current = (current + 1) % imgs.length;
                imgs[current].classList.add('active');
            }, 3000);
        }

        // 3. وظيفة القلوب
        function createHeart() {
            const h = document.createElement('div');
            h.classList.add('heart');
            h.innerHTML = '❤️';
            h.style.left = Math.random() * 100 + 'vw';
            h.style.animationDuration = (Math.random() * 2 + 3) + 's';
            document.body.appendChild(h);
            setTimeout(() => h.remove(), 5000);
        }

        // 4. الزرار السحري (الدخول)
        document.getElementById('goBtn').onclick = function() {
            const val = document.getElementById('passInput').value.trim();
            if (val === "15112021") {
                // إخفاء شاشة الدخول وإظهار المحتوى
                document.getElementById('login-screen').style.display = 'none';
                const main = document.getElementById('main-content');
                main.style.display = 'block';
                setTimeout(() => { main.style.opacity = '1'; }, 50);
                
                // تشغيل الأغنية والعداد والقلوب
                const vid = document.getElementById('youtube-vid');
                vid.src += "&autoplay=1";
                
                startImages();
                setInterval(updateCounter, 1000);
                setInterval(createHeart, 400);
                document.body.style.overflow = 'auto';
                window.scrollTo(0,0);
            } else {
                alert("الباسورد غلط يا روحي.. ركزي 😉");
            }
        };
    </script>
</body>
</html>

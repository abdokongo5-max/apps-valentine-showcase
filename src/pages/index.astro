---
// index.astro
---

<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>إلى أشرقت ❤️</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root { 
            --dark-bg: #000000; 
            --card-bg: #0a0a0a; 
        }
        
        /* منع الحركة الجانبية تماماً */
        html, body { 
            background-color: var(--dark-bg); 
            color: white; 
            font-family: 'Cairo', sans-serif; 
            margin: 0; 
            padding: 0;
            width: 100%;
            height: 100%;
            overflow-x: hidden; /* أهم سطر لمنع الشاشة تروح يمين وشمال */
            position: relative;
        }

        body {
            display: flex; 
            justify-content: center; 
            align-items: center; 
            min-height: 100vh;
        }

        #login-screen { 
            text-align: center; width: 85%; max-width: 380px; 
            background: rgba(15, 15, 15, 0.98); backdrop-filter: blur(15px);
            padding: 40px 25px; border-radius: 35px; 
            border: 1px solid rgba(255, 255, 255, 0.1);
            z-index: 100; position: relative;
            box-shadow: 0 0 30px rgba(255, 255, 255, 0.05);
        }

        #main-content { display: none; width: 90%; max-width: 550px; text-align: center; z-index: 10; padding: 40px 0; }

        .love-letter { background: rgba(255, 255, 255, 0.03); padding: 25px; border-radius: 25px; border: 1px solid rgba(255, 255, 255, 0.2); margin-bottom: 30px; line-height: 1.8; text-align: right; }

        .counter-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; direction: ltr; margin-bottom: 40px; }
        .counter-box { background: var(--card-bg); padding: 12px 5px; border-radius: 15px; border: 1px solid rgba(255, 255, 255, 0.1); }
        .counter-box span { display: block; font-size: 1.4rem; font-weight: bold; color: white; }
        .counter-box p { margin: 2px 0 0; font-size: 0.75rem; color: #888; }

        .photo-entry { margin-bottom: 40px; background: rgba(255, 255, 255, 0.02); padding: 15px; border-radius: 25px; border: 1px solid rgba(255, 255, 255, 0.1); }
        .photo-frame { width: 100%; border-radius: 20px; overflow: hidden; border: 1px solid #333; }
        .photo-frame img { width: 100%; display: block; }

        .input-box { width: 100%; padding: 15px; margin-bottom: 20px; border-radius: 50px; border: 1px solid #444; background: transparent; color: #fff; text-align: center; outline: none; box-sizing: border-box; font-size: 1.1rem; }
        .login-btn { width: 100%; padding: 15px; border-radius: 50px; border: none; background: white; color: black; font-weight: bold; cursor: pointer; font-size: 1.1rem; }

        /* الأنيميشن الثابت للقلوب */
        @keyframes heartFall {
            0% { transform: translateY(-10vh) rotate(0deg); opacity: 0; }
            10% { opacity: 1; }
            90% { opacity: 1; }
            100% { transform: translateY(110vh) rotate(360deg); opacity: 0; }
        }

        #music-player { position: absolute; width: 0; height: 0; overflow: hidden; }
    </style>
</head>
<body>

    <div id="login-screen">
        <h1>وحشتيني يا نور عيني ❤️</h1>
        <p>اكتبي تاريخنا يلا 😉💞</p>
        <input type="password" id="passInput" class="input-box" placeholder="اكتب تاريخنا ي روح قلبي">
        <button id="submitBtn" class="login-btn">ادخلي لقلبي 🔓</button>
    </div>

    <div id="main-content">
        <div id="music-player">
            <iframe id="youtube-audio" src="https://www.youtube.com/embed/XRUNVFhmD0o?enablejsapi=1&loop=1&playlist=XRUNVFhmD0o" allow="autoplay"></iframe>
        </div>
        <div class="love-letter">
            وحشتيني ي آش آش ❤️<br>
            وحشني صوتك وكلامك وضحكتك وهزارك حتي خناقك وحشني وحشتني كل حاجة فيكي عدا وقت كتير وكبير واحنا مش مع بعض بس انتي معايا دايمآ في تفكيري وقلبي وروحي وحشتيني يا اللي مشوفتش دقيقة حلوة في بعدك عني مشيتي وخدتي كل حاجة حلوة معاكي خدتي قلبي وعقلي وروحي بس انا بحبكك وهفضل احبكك لآخر ثانيه في عمري ولو فاضل في عمري دقيقة هعيشها ليكي ي روح قلبي من جوه.<br>
            <center><strong>توتاا قلبي 🌝❤️</strong></center>
        </div>
        <div class="counter-grid">
            <div class="counter-box"><span id="years">0</span><p>سنين</p></div>
            <div class="counter-box"><span id="months">0</span><p>شهور</p></div>
            <div class="counter-box"><span id="days">0</span><p>أيام</p></div>
            <div class="counter-box"><span id="hours">0</span><p>ساعات</p></div>
            <div class="counter-box"><span id="minutes">0</span><p>دقائق</p></div>
            <div class="counter-box"><span id="seconds">0</span><p>ثواني</p></div>
        </div>
        <div class="photo-entry"><div class="photo-frame"><img src="/1.jpg"></div><p>روح قلبي وعقلي آش آش ❤️</p></div>
        <div class="photo-entry"><div class="photo-frame"><img src="/2.jpg"></div><p>بحبكك يا قلب عبده 😍</p></div>
        <div class="photo-entry"><div class="photo-frame"><img src="/3.jpg"></div><p>بعشق امكك 😉💜</p></div>
        <div class="photo-entry"><div class="photo-frame"><img src="/4.jpg"></div><p>لو رجع بيا الزمن ألف مرة هختارك إنتي ❤️</p></div>
        <div class="photo-entry"><div class="photo-frame"><img src="/5.jpg"></div><p>يا ضحكة بتنور دنيتي ✨</p></div>
        <div class="photo-entry"><div class="photo-frame"><img src="/6.jpg"></div><p>دايماً في قلبي وبالي ♾️</p></div>
    </div>

    <script is:inline>
        function createHeart() {
            const heart = document.createElement('div');
            heart.innerHTML = '❤️';
            heart.style.position = 'fixed';
            heart.style.top = '-10vh';
            heart.style.left = Math.random() * 95 + 'vw';
            heart.style.color = 'white';
            heart.style.fontSize = (Math.random() * 15 + 10) + 'px';
            heart.style.zIndex = '1';
            heart.style.pointerEvents = 'none';
            heart.style.animation = `heartFall ${(Math.random() * 3 + 5)}s linear forwards`;
            
            document.body.appendChild(heart);
            setTimeout(() => { heart.remove(); }, 8000);
        }

        setInterval(createHeart, 500);

        function updateCounter() {
            const startDate = new Date("2021-11-15T00:00:00");
            const now = new Date();
            const diff = now - startDate;
            document.getElementById("years").innerText = Math.floor(diff / (1000 * 60 * 60 * 24 * 365.25));
            document.getElementById("months").innerText = Math.floor((diff % (1000 * 60 * 60 * 24 * 365.25)) / (1000 * 60 * 60 * 24 * 30.44));
            document.getElementById("days").innerText = Math.floor((diff % (1000 * 60 * 60 * 24 * 30.44)) / (1000 * 60 * 60 * 24));
            document.getElementById("hours").innerText = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            document.getElementById("minutes").innerText = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
            document.getElementById("seconds").innerText = Math.floor((diff % (1000 * 60)) / 1000);
        }

        document.getElementById('submitBtn').addEventListener('click', function() {
            const pass = document.getElementById('passInput').value.trim();
            if (pass === "15112021") {
                document.getElementById('login-screen').style.display = 'none';
                document.getElementById('main-content').style.display = 'block';
                document.body.style.display = 'block';
                const iframe = document.getElementById('youtube-audio');
                iframe.src += "&autoplay=1";
                setInterval(updateCounter, 1000);
                updateCounter();
                window.scrollTo(0, 0);
            } else { alert("الباسورد غلط يا روحي 😉"); }
        });
    </script>
</body>
</html>

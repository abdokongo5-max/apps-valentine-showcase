---
// index.astro
---

<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إلى أشرقت ❤️</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --main-purple: #ff00ff;
            --dark-bg: #0d0221;
            --card-bg: #1a1a2e;
        }

        body {
            background-color: var(--dark-bg);
            color: white;
            font-family: 'Cairo', sans-serif;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* --- شاشة الدخول --- */
        #login-screen {
            text-align: center;
            width: 90%;
            max-width: 350px;
            background: var(--card-bg);
            padding: 40px 30px;
            border-radius: 35px;
            box-shadow: 0 0 30px rgba(255, 0, 255, 0.2);
        }

        .input-box {
            width: 100%;
            padding: 15px;
            margin: 20px 0;
            border-radius: 50px;
            border: 2px solid var(--main-purple);
            background: transparent;
            color: #fff;
            text-align: center;
            font-size: 1.1rem;
            outline: none;
            box-sizing: border-box;
        }

        .login-btn {
            width: 100%;
            padding: 15px;
            border-radius: 50px;
            border: none;
            background: linear-gradient(90deg, #ff00ff, #8a2be2);
            color: #fff;
            font-weight: bold;
            font-size: 1.1rem;
            cursor: pointer;
            box-shadow: 0 5px 15px rgba(255, 0, 255, 0.4);
        }

        /* --- شاشة المحتوى الرئيسي --- */
        #main-content {
            display: none;
            width: 95%;
            max-width: 600px;
            text-align: center;
            padding: 20px 0;
        }

        .photo-frame {
            width: 100%;
            height: 400px;
            border: 3px solid var(--main-purple);
            border-radius: 25px;
            margin-bottom: 20px;
            overflow: hidden;
            position: relative;
        }

        .photo-frame img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            position: absolute;
            left: 0;
            opacity: 0;
            transition: 1s ease-in-out;
        }

        .photo-frame img.active {
            opacity: 1;
        }

        iframe {
            width: 100%;
            height: 300px;
            border-radius: 20px;
            border: none;
            margin: 20px 0;
        }

        .message-box {
            background: rgba(255, 255, 255, 0.05);
            padding: 25px;
            border-radius: 20px;
            border-right: 5px solid var(--main-purple);
            text-align: right;
            line-height: 1.8;
            font-size: 1.15rem;
        }

        h1 { color: var(--main-purple); }
    </style>
</head>
<body>

    <div id="login-screen">
        <h1>عيد الحب 💕</h1>
        <p style="color: #b3b3b3;">جاهزة يا روحي؟ 💞</p>
        <input type="password" id="passInput" class="input-box" placeholder="اكتبي تاريخنا يا بطوطتي 😊❤️">
        <button id="submitBtn" class="login-btn">ادخلي يا نور عيني 💜</button>
    </div>

    <div id="main-content">
        <h1>أشرقت.. ❤️</h1>
        <div class="photo-frame">
            <img src="/1.jpg" class="active">
            <img src="/2.jpg">
            <img src="/3.jpg">
            <img src="/4.jpg">
        </div>

        <iframe src="https://www.youtube.com/embed/n7Tmcj8E6qQ" allowfullscreen></iframe>

        <div class="message-box">
            أشرقت يعني قلبي وروحي وعقلي يعني حياتي اللي ملهاش طعم غير بيها ومتكملش غير بيها ولا يبقي ليها طعم من غيرها ولا اعرف اعيش من غيرها.. <br><br>
            من اول يوم شوفتك فيه وانا فاكر اليوم بالدقيقة والثانية احلي صدفة في عمري واجمل لحظة جمعتني بيكي.. <br><br>
            ولو رجع بيا الزمن الف مرة وخيروني بينك وبين الدنيا كلها هختارك انتي في كل مرة.. <br><br>
            بحبك يا اللي مشوفتش الحب وعرفته غير على إيديكي ❤️
        </div>
    </div>

    <script is:inline>
        // دالة الدخول
        function checkPassword() {
            const input = document.getElementById('passInput');
            const loginScreen = document.getElementById('login-screen');
            const mainContent = document.getElementById('main-content');

            if (input && loginScreen && mainContent) {
                if (input.value === "15112021") {
                    loginScreen.style.display = 'none';
                    mainContent.style.display = 'block';
                    startSlider();
                } else {
                    alert("الباسورد غلط يا روحي، ركزي في تاريخنا 😉");
                }
            }
        }

        // دالة تحريك الصور
        function startSlider() {
            const images = document.querySelectorAll('.photo-frame img');
            let index = 0;
            if (images.length > 0) {
                setInterval(() => {
                    images[index].classList.remove('active');
                    index = (index + 1) % images.length;
                    images[index].classList.add('active');
                }, 3000);
            }
        }

        // ربط الزرار بالكود عند تحميل الصفحة
        document.getElementById('submitBtn')?.addEventListener('click', checkPassword);
    </script>
</body>
</html>

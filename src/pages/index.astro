---
// index.astro
---

<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إلى أشرقت ❤️</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root { --main-purple: #ff00ff; --dark-bg: #0d0221; --card-bg: #1a1a2e; }
        body { background-color: var(--dark-bg); color: white; font-family: 'Cairo', sans-serif; margin: 0; display: flex; justify-content: center; align-items: center; min-height: 100vh; overflow: hidden; }
        
        #login-screen { text-align: center; width: 90%; max-width: 350px; background: var(--card-bg); padding: 40px 30px; border-radius: 35px; box-shadow: 0 0 30px rgba(255, 0, 255, 0.2); position: relative; z-index: 10; }
        .input-box { width: 100%; padding: 15px; margin: 20px 0; border-radius: 50px; border: 2px solid var(--main-purple); background: transparent; color: #fff; text-align: center; font-size: 1.1rem; outline: none; box-sizing: border-box; }
        .login-btn { width: 100%; padding: 15px; border-radius: 50px; border: none; background: linear-gradient(90deg, #ff00ff, #8a2be2); color: #fff; font-weight: bold; font-size: 1.1rem; cursor: pointer; box-shadow: 0 5px 15px rgba(255, 0, 255, 0.4); }

        #main-content { display: none; text-align: center; width: 90%; max-width: 500px; animation: fadeIn 1.5s ease-in; }
        .slider { position: relative; width: 100%; height: 350px; border-radius: 25px; overflow: hidden; border: 3px solid var(--main-purple); box-shadow: 0 0 25px var(--main-purple); margin-bottom: 25px; }
        .slider img { position: absolute; width: 100%; height: 100%; object-fit: cover; opacity: 0; transition: opacity 1s ease-in-out; }
        .slider img.active { opacity: 1; }

        .video-container { width: 100%; max-width: 315px; margin: 20px auto; border-radius: 20px; overflow: hidden; box-shadow: 0 0 15px rgba(255,0,255,0.3); }
        iframe { width: 100%; height: 560px; border: none; }

        h1 { color: var(--main-purple); text-shadow: 0 0 10px var(--main-purple); }
        p { font-size: 1.2rem; line-height: 1.6; }

        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    </style>
</head>
<body>

    <div id="login-screen">
        <h1>عيد الحب 💕</h1>
        <p style="color: #b3b3b3;">جاهزة يا روحي؟ 💞</p>
        <input type="password" id="passInput" class="input-box" placeholder="اكتبي تاريخنا يا بطوطتي 😊❤️">
        <button id="submitBtn" class="login-btn">ادخلي يا نور عيني 💜</button>
    </div>

    <div id="main-content">
        <h1>أشرقت.. ❤️</h1>
        <div class="slider" id="slider">
            <img src="/1.jpg" class="active">
            <img src="/2.jpg">
            <img src="/3.jpg">
            <img src="/4.jpg">
        </div>

        <div class="video-container">
            <iframe id="video" src="https://www.youtube.com/embed/n7Tmcj8E6qQ" allow="autoplay; encrypted-media" allowfullscreen></iframe>
        </div>

        <p>أشرقت يعني قلبي وروحي وعقلي.. <br> كل سنة وإنتي معايا يا أجمل حاجة في دنيتي ❤️</p>
    </div>

    <script is:inline>
        let currentSlide = 0;
        function startSlider() {
            const images = document.querySelectorAll('.slider img');
            setInterval(() => {
                images[currentSlide].classList.remove('active');
                currentSlide = (currentSlide + 1) % images.length;
                images[currentSlide].classList.add('active');
            }, 3000);
        }

        function checkPassword() {
            const input = document.getElementById('passInput');
            if (input && input.value === "15112021") {
                document.getElementById('login-screen').style.display = 'none';
                document.getElementById('main-content').style.display = '

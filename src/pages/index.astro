---
// index.astro
---

<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="theme-color" content="#0d0221">
    <title>إلى أشرقت ❤️</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root { --main-purple: #ff00ff; --dark-bg: #0d0221; --card-bg: #1a1a2e; }
        body { background-color: var(--dark-bg); color: white; font-family: 'Cairo', sans-serif; margin: 0; display: flex; justify-content: center; align-items: center; min-height: 100vh; overflow-x: hidden; }
        
        #login-screen { text-align: center; width: 90%; max-width: 350px; background: var(--card-bg); padding: 40px 30px; border-radius: 35px; box-shadow: 0 0 30px rgba(255, 0, 255, 0.2); position: fixed; z-index: 100; }
        .input-box { width: 100%; padding: 15px; margin: 20px 0; border-radius: 50px; border: 2px solid var(--main-purple); background: transparent; color: #fff; text-align: center; font-size: 1.1rem; outline: none; box-sizing: border-box; }
        .login-btn { width: 100%; padding: 15px; border-radius: 50px; border: none; background: linear-gradient(90deg, #ff00ff, #8a2be2); color: #fff; font-weight: bold; cursor: pointer; }

        #main-content { display: none; width: 95%; max-width: 600px; text-align: center; padding: 40px 0; animation: fadeIn 1.5s ease-in; }
        
        /* تصميم العداد التنازلي */
        .counter-section { margin-bottom: 40px; }
        .counter-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; direction: ltr; margin-top: 20px; }
        .counter-box { background: var(--card-bg); padding: 15px 5px; border-radius: 15px; border: 1px solid rgba(255, 0, 255, 0.2); box-shadow: 0 4px 15px rgba(0,0,0,0.3); }
        .counter-box span { display: block; font-size: 1.5rem; font-weight: bold; color: var(--main-purple); }
        .counter-box p { margin: 5px 0 0; font-size: 0.75rem; color: #b3b3b3; }

        .photo-entry { margin-bottom: 50px; background: rgba(255, 255, 255, 0.03); padding: 20px; border-radius: 30px; border: 1px solid rgba(255, 0, 255, 0.1); }
        .photo-frame { width: 100%; border-radius: 20px; overflow: hidden; border: 2px solid var(--main-purple); box-shadow: 0 0 15px var(--main-purple); }
        .photo-frame img { width: 100%; display: block; }
        .photo-caption { margin-top: 20px; font-size: 1.3rem; font-weight: bold; text-shadow: 0 0 10px var(--main-purple); }
        
        .video-container { width: 100%; max-width: 315px; margin: 40px auto; border-radius: 25px; overflow: hidden; box-shadow: 0 0 20px rgba(255,0,255,0.4); }
        iframe { width: 100%; height: 560px; border: none; }
        
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    </style>
</head>
<body>

    <div id="login-screen">
        <h1>عيد الحب 💕</h1>
        <p style="color: #b3b3b3;">جاهزة يا روحي؟ 💞</p>
        <input type="password" id="passInput" class="input-box" placeholder="اكتبي تاريخنا يا بطوطتي 😊❤️">
        <button id="submitBtn" class="login-btn">ادخلي يا نور عيني 💜</button>
    </div>

    <div id="main-content">
        <h1 style="margin-bottom: 10px;">أشرقت.. ❤️</h1>
        
        <div class="counter-section">
            <p style="font-size: 1.1rem; color: #e0e0e0;">بقالي معاكي أجمل:</p>
            <div class="counter-grid">
                <div class="counter-box"><span id="years">0</span><p>سنين</p></div>
                <div class="counter-box"><span id="months">0</span><p>شهور</p></div>
                <div class="counter-box"><span id="days">0</span><p>أيام</p></div>
                <div class="counter-box"><span id="hours">0</span><p>ساعات</p></div>
                <div class="counter-box"><span id="minutes">0</span><p>دقائق</p></div>
                <div class="counter-box"><span id="seconds">0</span><p>ثواني</p></div>
            </div>
        </div>

        <div class="photo-entry">
            <div class="photo-frame"><img src="/1.jpg"></div>
            <p class="photo-caption">روح قلبي وعقلي آش آش ❤️</p>
        </div>
        <div class="photo-entry">
            <div class="photo-frame"><img src="/2.jpg"></div>
            <p class="photo-caption">بحبكك يا قلب عبده 😍</p>
        </div>
        <div class="photo-entry">
            <div class="photo-frame"><img src="/3.jpg"></div>
            <p class="photo-caption">بعشق امكك 😉💜</p>
        </div>
        <div class="photo-entry">
            <div class="photo-frame"><img src="/4.jpg"></div>
            <p class="photo-caption">
                أشرقت يعني قلبي وروحي وعقلي يعني حياتي اللي ملهاش طعم غير بيها ومتكملش غير بيها.. <br><br>
                لو رجع بيا الزمن الف مرة هختارك انتي في كل مرة.. بحبك ❤️
            </p>
        </div>

        <div class="video-container">
            <iframe src="https://www.youtube.com/embed/n7Tmcj8E6qQ?autoplay=1" allow="autoplay; encrypted-media" allowfullscreen></iframe>
        </div>
    </div>

    <script is:inline>
        function updateCounter() {
            const startDate = new Date("2021-11-15T00:00:00");
            const now = new Date();
            const diff = now - startDate;

            const years = Math.floor(diff / (1000 * 60 * 60 * 24 * 365.25));
            const months = Math.floor((diff % (1000 * 60 * 60 * 24 * 365.25)) / (1000 * 60 * 60 * 24 * 30.44));
            const days = Math.floor((diff % (1000 * 60 * 60 * 24 * 30.44)) / (1000 * 60 * 60 * 24));
            const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((diff % (1000 * 60)) / 1000);

            document.getElementById("years").innerText = years;
            document.getElementById("months").innerText = months;
            document.getElementById("days").innerText = days;
            document.getElementById("hours").innerText = hours;
            document.getElementById("minutes").innerText = minutes;
            document.getElementById("seconds").innerText = seconds;
        }

        function checkPassword() {
            const input = document.getElementById('passInput');
            if (input && input.value === "15112021") {
                document.getElementById('login-screen').style.display = 'none';
                document.getElementById('main-content').style.display = 'block';
                document.body.style.alignItems = 'flex-start';
                window.scrollTo(0, 0);
                setInterval(updateCounter, 1000);
                updateCounter();
            } else {
                alert("الباسورد غلط يا روحي 😉");
            }
        }
        document.getElementById('submitBtn')?.addEventListener('click', checkPassword);
    </script>
</body>
</html>

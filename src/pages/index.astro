---
---
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Valentine's Day ❤️</title>
</head>
<body style="margin:0; background-color:#fff0f3; font-family:sans-serif; display:flex; align-items:center; justify-content:center; min-height:100vh; overflow-x:hidden;">

    <div id="lock" style="text-align:center; padding:25px; background:white; border-radius:20px; box-shadow:0 10px 25px rgba(255,77,109,0.1); width:90%; max-width:320px; border:1px solid #ffccd5; z-index: 100;">
        <div style="font-size:50px; margin-bottom:10px;">🔒</div>
        <h2 style="color:#ff4d6d; margin:10px 0;">المنطقة دي لقلب عبده بس</h2>
        <input type="password" id="pass" placeholder="التاريخ السري.." style="width:100%; padding:12px; margin-bottom:15px; border:2px solid #ffccd5; border-radius:10px; text-align:center; outline:none; font-size:16px;">
        <button id="go" style="width:100%; padding:12px; background:#ff4d6d; color:white; border:none; border-radius:10px; cursor:pointer; font-weight:bold; width:100%;">دخول</button>
        <p id="err" style="color:#ff4d6d; display:none; margin-top:15px; font-weight:bold;">التاريخ غلط يا توتا.. ركزي 😉</p>
    </div>

    <div id="show" style="display:none; text-align:center; padding:20px; width:100%; max-width:450px;">
        <h1 style="color:#ff4d6d;">Happy Valentine's Day ❤️</h1>
        
        <div style="background:white; padding:10px; border-radius:20px; box-shadow:0 10px 20px rgba(0,0,0,0.05);">
            <img id="main-img" src="https://i.postimg.cc/LhFp1cws/our-photo-1.jpg" 
                 style="width:100%; border-radius:15px; display:block; max-height:450px; object-fit:cover;">
        </div>

        <div style="background:white; padding:20px; border-radius:20px; margin-top:20px; border:1px solid #ffccd5;">
            <p style="font-size:18px; color:#444; line-height:1.6;">
                حبيت حتى واحنا بعاد عن بعض لازم أفرحك بحاجة وأعرفك إنك مهما كنتي بعيدة عني قريبة من قلبي وروحي.. 
                <br><br>
                <span style="color:#ff4d6d; font-weight:bold; font-size:20px;">Will you be my valentine? 🥺</span>
            </p>
            
            <div id="btns-container" style="margin-top:25px; display:flex; justify-content:center; gap:15px; min-height:60px; position:relative;">
                <button id="yesBtn" style="padding:12px 35px; background:#ff4d6d; color:white; border:none; border-radius:10px; font-weight:bold; cursor:pointer;">Yes</button>
                <button id="noBtn" style="padding:12px 35px; background:#eee; color:#888; border:none; border-radius:10px; cursor:pointer;">No</button>
            </div>
            <div id="final" style="display:none; margin-top:20px; color:#ff4d6d; font-weight:bold; font-size:18px; padding:10px; border:1px dashed #ff4d6d; border-radius:10px;">بحبك ي توتاا ي قلب عبده من جوه ❤️</div>
        </div>
    </div>

    <script is:inline>
        const goBtn = document.getElementById('go');
        const passInput = document.getElementById('pass');
        const lockDiv = document.getElementById('lock');
        const showDiv = document.getElementById('show');
        const errTag = document.getElementById('err');
        const yesBtn = document.getElementById('yesBtn');
        const noBtn = document.getElementById('noBtn');
        const finalDiv = document.getElementById('final');
        const btnsContainer = document.getElementById('btns-container');

        if (goBtn && passInput) {
            goBtn.onclick = function() {
                if (passInput.value === '15112021') {
                    if (lockDiv) lockDiv.style.display = 'none';
                    if (showDiv) showDiv.style.display = 'block';
                } else {
                    if (errTag) errTag.style.display = 'block';
                }
            };
        }

        if (yesBtn) {
            yesBtn.onclick = function() {
                if (finalDiv) finalDiv.style.display = 'block';
                if (btnsContainer) btnsContainer.style.display = 'none';
            };
        }

        if (noBtn) {
            const moveNo = () => {
                noBtn.style.position = 'fixed';
                noBtn.style.left = Math.random() * (window.innerWidth - 100) + 'px';
                noBtn.style.top = Math.random() * (window.innerHeight - 50) + 'px';
                noBtn.style.zIndex = '999';
            };
            noBtn.onmouseover = moveNo;
            noBtn.ontouchstart = (e) => { e.preventDefault(); moveNo(); };
        }
    </script>
</body>
</html>

---
// index.astro
---

<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إلى أشرقت ❤️</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root { --main-purple: #ff00ff; --dark-bg: #0d0221; --card-bg: #1a1a2e; }
        
        /* نفس ستايل الخلفية اللي في اللينك */
        body { 
            background-color: var(--dark-bg); 
            color: white; 
            font-family: 'Cairo', sans-serif; 
            margin: 0; 
            display: flex; 
            justify-content: center; 
            align-items: center; 
            min-height: 100vh; 
            overflow-x: hidden; 
        }
        
        /* نفس شكل شاشة الدخول الموف */
        #login-screen { 
            text-align: center; 
            width: 90%; 
            max-width: 350px; 
            background: var(--card-bg); 
            padding: 40px 30px; 
            border-radius: 35px; 
            box-shadow: 0 0 30px rgba(255, 0, 255, 0.2); 
            position: fixed; 
            z-index: 100; 
        }
        
        .input-box { 
            width: 100%; 
            padding: 15px; 
            margin: 20px 0; 
            border-radius: 50px; 
            border: 2px solid var(--main-purple); 
            background: transparent; 
            color: #fff; 
            text-align: center; 
            font-size: 1.1rem; 
            outline: none; 
            box-sizing: border-box; 
        }

        .login-btn { 
            width: 100%; 
            padding: 15px; 
            border-radius: 50px; 
            border: none; 
            background: linear-gradient(90deg, #ff00ff, #8a2be2); 
            color: #fff; 
            font-weight: bold; 
            font-size: 1.1rem; 
            cursor: pointer; 
            box-shadow: 0 5px 15px rgba(255, 0, 255, 0.4); 
        }

        /* شاشة المحتوى - عرض طولي لكل صورة لوحدها */
        #main-content { 
            display: none; 
            width: 95%; 
            max-width: 600px; 
            text-align: center; 
            padding: 40px 0; 
            animation: fadeIn 1.5s ease-in; 
        }
        
        .photo-entry { 
            margin-bottom: 50px; 
            background: rgba(255, 255, 255, 0.03); 
            padding: 20px; 
            border-radius: 30px; 
            border: 1px solid rgba(255, 0, 255, 0.1); 
        }

        .photo-frame { 
            width: 100%; 
            border-radius: 20px; 
            overflow: hidden; 
            border: 2px solid var(--main-purple); 
            box-shadow: 0 0 15px var(--main-purple); 
        }

        .photo-frame img { width: 100%; display: block; object-fit: cover; }

        /* ستايل الجمل تحت الصور */
        .photo-caption { 
            margin-top: 20px; 
            font-size: 1.4rem; 
            color: #fff; 
            line-height: 1.6; 
            font-weight: bold; 
            text-shadow: 0 0 10px var(--main-purple); 
        }

        .video-container { 
            width: 100%; 
            max-width: 315px; 
            margin: 40px auto; 
            border-radius: 25px; 
            overflow: hidden; 
            box-shadow: 0 0 20px rgba(255,0,255,0.4); 
        }

        iframe { width: 100%; height: 560px; border: none; }

        h1 { color: var(--main-purple); text-shadow: 0 0 15px var(--main-purple); margin-bottom: 30px; }

        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    </style>
</head>
<body>

    <div id="login-screen">
        <h1>عيد الحب 💕</h1>
        <p style="color: #b3b3b3;">جاهزة يا روحي؟ 💞</p>
        <input type="password" id="passInput" class="input-box" placeholder="اكتبي تاريخنا يا بطوطتي 😊❤️">
        <button id="submitBtn" class="login-btn">ادخلي يا نور عيني 💜</button>
    </div>

    <div id="main-content">
        <h1>أشرقت.. ❤️</h1>

        <div class="photo-entry">
            <div class="photo-frame"><img src="/1.jpg"></div>
            <p class="photo-caption">روح قلبي وعقلي آش آش ❤️</p>
        </div>

        <div class="photo-entry">
            <div class="photo-frame"><img src="/2.jpg"></div>
            <p class="photo-caption">بحبكك يا قلب عبده 😍</p>
        </div>

        <div class="photo-entry">
            <div class="photo-frame"><img src="/3.jpg"></div>
            <p class="photo-caption">بعشق امكك 😉💜</p>
        </div>

        <div class="photo-entry">
            <div class="photo-frame"><img src="/4.jpg"></div>
            <p class="photo-caption">
                أشرقت يعني قلبي وروحي وعقلي يعني حياتي اللي ملهاش طعم غير بيها ومتكملش غير بيها ولا يبقي ليها طعم من غيرها ولا اعرف اعيش من غيرها.. <br><br>
                لو رجع بيا الزمن الف مرة وخيروني بينك وبين الدنيا كلها هختارك انتي في كل مرة.. بحبك ❤️
            </p>
        </div>

        <div class="video-container">
            <iframe src="https://www.youtube.com/embed/n7Tmcj8E6qQ?autoplay=1" allow="autoplay; encrypted-media" allowfullscreen></iframe>
        </div>

    </div>

    <script is:inline>
        function checkPassword() {
            const input = document.getElementById('passInput');
            if (input && input.value === "15112021") {
                document.getElementById('login-screen').style.display = 'none';
                document.getElementById('main-content').style.display = 'block';
                // تعديل عشان الصفحة تنفع تنزل لتحت (Scroll) وتشوف باقي الصور
                document.body.style.alignItems = 'flex-start'; 
                window.scrollTo(0, 0);
            } else {
                alert("الباسورد غلط يا روحي 😉");
            }
        }
        document.getElementById('submitBtn')?.addEventListener('click', checkPassword);
    </script>
</body>
</html>

---
---
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Valentine ❤️</title>
</head>
<body style="margin:0; background:#fff0f3; font-family:sans-serif; display:flex; align-items:center; justify-content:center; min-height:100vh;">

    <div id="lock" style="text-align:center; padding:20px; background:white; border-radius:20px; box-shadow:0 10px 20px rgba(0,0,0,0.1); width:90%; max-width:300px;">
        <h2 style="color:#ff4d6d;">🔒 سر لتوتا بس</h2>
        <input type="password" id="pass" placeholder="التاريخ السري.." style="width:100%; padding:10px; margin:10px 0; border:2px solid #ffccd5; border-radius:10px; text-align:center;">
        <button id="go" style="width:100%; padding:10px; background:#ff4d6d; color:white; border:none; border-radius:10px; cursor:pointer;">دخول</button>
        <p id="err" style="color:red; display:none;">غلط يا توتا 😉</p>
    </div>

    <div id="show" style="display:none; text-align:center; padding:20px;">
        <h1 style="color:#ff4d6d;">Happy Valentine's Day ya Ash Ash ❤️</h1>
        <img src="/images/our-photo.jpg" style="width:100%; max-width:400px; border-radius:20px; border:5px solid white;">
        <p style="background:white; padding:15px; border-radius:15px; margin:15px 0;">حبيت واحنا بعاد افرحك.. <br><b>Will you be my valentine?</b></p>
        <button onclick="alert('بحبك يا توتا ❤️')" style="padding:10px 20px; background:#ff4d6d; color:white; border:none; border-radius:10px;">Yes</button>
        <button id="no" style="padding:10px 20px; background:#ccc; border:none; border-radius:10px; margin-right:10px;">No</button>
    </div>

    <script>
        const passInput = document.getElementById('pass') as HTMLInputElement;
        const lock = document.getElementById('lock');
        const show = document.getElementById('show');
        const noBtn = document.getElementById('no');

        document.getElementById('go')?.addEventListener('click', () => {
            if (passInput.value === '15112021') {
                if(lock) lock.style.display = 'none';
                if(show) show.style.display = 'block';
            } else {
                const err = document.getElementById('err');
                if(err) err.style.display = 'block';
            }
        });

        noBtn?.addEventListener('mouseover', () => {
            noBtn.style.position = 'absolute';
            noBtn.style.left = Math.random() * 80 + '%';
            noBtn.style.top = Math.random() * 80 + '%';
        });
    </script>
</body>
</html>

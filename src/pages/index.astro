---
---
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Valentine's Day My Love ❤️</title>
    <style>
        body, html {
            margin: 0; padding: 0;
            background: linear-gradient(135deg, #fff0f3 0%, #ffccd5 100%);
            font-family: 'Segoe UI', Tahoma, sans-serif;
            scroll-behavior: smooth;
        }
        #main-container {
            display: flex; flex-direction: column; align-items: center; min-height: 100vh; padding: 20px;
        }
        .card {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            padding: 20px; border-radius: 25px;
            box-shadow: 0 10px 25px rgba(255, 77, 109, 0.15);
            width: 90%; max-width: 450px; text-align: center;
            margin-bottom: 30px; border: 1px solid rgba(255, 255, 255, 0.5);
        }
        .photo-frame {
            width: 100%; border-radius: 20px; border: 5px solid white;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1); margin-bottom: 15px;
        }
        .heart {
            position: fixed; bottom: -20px; font-size: 20px; color: #ff4d6d;
            animation: float 5s linear forwards; z-index: 50;
        }
        @keyframes float { 0% { transform: translateY(0); opacity: 1; } 100% { transform: translateY(-100vh); opacity: 0; } }
        input { width: 100%; padding: 15px; border-radius: 12px; border: 2px solid #ffccd5; margin: 15px 0; outline: none; text-align: center; }
        button { background: #ff4d6d; color: white; border: none; padding: 15px 30px; border-radius: 12px; font-weight: bold; cursor: pointer; width: 100%; }
        #noBtn { background: #eee; color: #888; width: auto; position: relative; }
        .hidden { display: none; }
    </style>
</head>
<body>

    <audio id="bgMusic" loop>
        <source src="https://files.catbox.moe/860vum.mp3" type="audio/mpeg">
    </audio>

    <div id="main-container">
        
        <div id="lock" class="card">
            <div style="font-size:50px;">🔒</div>
            <h2 style="color:#ff4d6d;">جاهزة للمفاجأة؟</h2>
            <p>اكتبي تاريخ احسن يوم في عمري 😉</p>
            <input type="text" id="pass" placeholder="أدخلي التاريخ هنا..">
            <button id="go">افتحي قلبي ❤️</button>
            <p id="err" class="hidden" style="color:#ff4d6d; font-weight:bold; margin-top:10px;">التاريخ غلط يا توتا.. ركزي 🥺</p>
        </div>

        <div id="album" class="hidden" style="width: 100%; display: flex; flex-direction: column; align-items: center;">
            <h1 style="color:#ff4d6d; text-align:center;">Happy Valentine's Day ❤️</h1>

            <div class="card">
                <img src="https://i.postimg.cc/pL4dZL4M/our-photo.jpg" class="photo-frame">
                <p style="color:#444; font-weight:bold;">كل لحظة معاكي هي بداية لحكاية حب جديدة.. ❤️</p>
            </div>

            <div class="card">
                <img src="https://i.postimg.cc/6pgQ0pgX/our-photo.jpg" class="photo-frame">
                <p style="color:#444; font-weight:bold;">ضحكتك هي اللي بتنور أيامي الصعبة.. 😍</p>
            </div>

            <div class="card">
                <img src="https://i.postimg.cc/wjZBVjPZ/our-photo-2.jpg" class="photo-frame">
                <p style="color:#444; font-weight:bold;">أجمل صدفة في حياتي هي اليوم اللي عرفتك فيه.. ✨</p>
            </div>

            <div class="card">
                <img src="https://i.postimg.cc/6pgQ0pgK/Screenshot-20260215-061957-Contacts.jpg" class="photo-frame">
                <p style="color:#444; font-weight:bold;">يا رب نفضل سوا طول العمر وما يفرقنا شيء.. 💍</p>
            </div>

            <div class="card">
                <h3 style="color:#ff4d6d;">Will you be my valentine? 🥺</h3>
                <div style="display:flex; justify-content:center; gap:20px;">
                    <button id="yesBtn" style="width:auto;">Yes 😍</button>
                    <button id="noBtn">No 😢</button>
                </div>
                <div id="final" class="hidden" style="margin-top:20px; color:#ff4d6d; font-weight:bold; border:2px dashed #ff4d6d; padding:15px; border-radius:15px;">
                    بحبك ي توتاا ي قلب عبده من جوه ❤️💍
                </div>
            </div>
        </div>
    </div>

    <script is:inline>
        const music = document.getElementById('bgMusic');
        
        document.getElementById('go').onclick = function() {
            const pass = document.getElementById('pass').value.trim();
            if (pass === '15112021') {
                // تفعيل الأغنية فوراً
                music.play().catch(() => {
                    // محاولة بديلة لو المتصفح رفض
                    window.addEventListener('click', () => music.play(), {once: true});
                });

                document.getElementById('lock').classList.add('hidden');
                document.getElementById('album').classList.remove('hidden');
                document.body.style.overflow = 'auto'; // السماح بالسكورول للألبوم
                
                // بدء القلوب
                setInterval(() => {
                    const heart = document.createElement('div');
                    heart.innerHTML = '❤️'; heart.className = 'heart';
                    heart.style.left = Math.random() * 100 + 'vw';
                    heart.style.animationDuration = (Math.random() * 2 + 3) + 's';
                    document.body.appendChild(heart);
                    setTimeout(() => heart.remove(), 5000);
                }, 400);
            } else {
                document.getElementById('err').classList.remove('hidden');
            }
        };

        document.getElementById('yesBtn').onclick = function() {
            document.getElementById('final').classList.remove('hidden');
            music.play();
        };

        const noBtn = document.getElementById('noBtn');
        const moveNo = () => {
            noBtn.style.position = 'fixed';
            noBtn.style.left = Math.random() * 80 + 'vw';
            noBtn.style.top = Math.random() * 80 + 'vh';
        };
        noBtn.onmouseover = moveNo;
        noBtn.ontouchstart = moveNo;
    </script>
</body>
</html>
